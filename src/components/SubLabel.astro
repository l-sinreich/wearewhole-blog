---
/**
 * SubLabel.astro
 *
 * A small section sub-heading used inside a line of inquiry — e.g.
 * "Where the gap shows up". Shares the exact treatment of the
 * "What we're watching" header (.wp-sublabel) so the two read as peers.
 */
---
<p class="wp-sublabel"><slot /></p>
