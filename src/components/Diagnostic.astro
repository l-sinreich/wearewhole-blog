---
/**
 * Diagnostic.astro
 *
 * The pull-quote / diagnostic question under each line of inquiry — the
 * conversion device. No quote marks; the visual weight does the work.
 * It is meant to breathe, so it gets generous vertical space.
 *
 * Usage in MDX:
 *   <Diagnostic>Think of a recent decision that mattered…</Diagnostic>
 */
---
<blockquote class="wp-diagnostic">
  <slot />
</blockquote>
