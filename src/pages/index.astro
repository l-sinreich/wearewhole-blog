---
// Redirect / → /blog/ using Astro's built-in redirect.
// This is a proper HTTP 301 redirect, not a meta-refresh.
// A meta-refresh fires after the page loads and can confuse browsers
// into applying the redirect to other cached pages on the same domain.
return Astro.redirect('/blog/', 301);
---
